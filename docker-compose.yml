services:
  telegram-contract-bot:
    build: .
    container_name: telegram-contract-bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      DATA_DIR: /app/data
    volumes:
      - ./data:/app/data
      - ./contracts:/app/contracts
      - ./tmp_photos:/app/tmp_photos
